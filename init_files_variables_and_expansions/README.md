# Init Files, Variables and Expansions

This project contains Bash scripts that demonstrate shell initialization files, aliases, variables, expansions, and related Bash concepts.

## Scripts

- `0-alias` - Creates an alias named `ls` with the value `rm *`.
